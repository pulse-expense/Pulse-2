# Pulse Validation

This checkpoint exists to trigger CI after the latest feature work. The release gate remains: build must pass, then runtime/Supabase/mobile validation must be completed before merging `react-modernization` into `main`.
